# Stockron Analyzer v12 — Render Backend
FastAPI API שמחזיר /health ו-/analyze לפי מבנה נתונים קבוע ל-Base44.

▸ Endpoints:
  - GET /health
  - POST /analyze  { "ticker": "NVDA" }

▸ הפעלה ב-Render:
  - Manual Deploy → Deploy latest commit
  - בדיקה: פתח /health ו-/analyze (POST) מהדפדפן / reqbin
